import streamlit as st 
st.title("wellcome to streamlit")
#print(5+6)
num1=st.number_input("enter number 1")


num2=st.number_input("enter number 2")
sum=num1+num2
st.button("addition")
st.write("sum is sum",sum)
num=st.number_input("enter number 1",step=1)
num=st.number_input("enter number 2",step=1)

sum=num1+num2
if st.button("add"):
    st.write("sum is: ",sum)

sub=num1-num2
if st.button("sub"):
    st.writ("sub is:",sub)


#import stream;lit as st
st.title ("wellcome to streamlit")
username=st.text_input("enter username")
passward=st.text_input("enter passwaed")
if st.button("login"):
    if username=="admin":
        st.success("valid user")
    else:
        st.error("invalid password")
else:
   st.error("invalid userneme")















































































































































































































































































































































































































































































































































































































































